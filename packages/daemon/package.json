{
  "name": "@emilswork/harness-daemon",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP and WebSocket API, run supervisor, SQLite event log, and the host of the built UI.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "bin": {
    "dsh-daemon": "./dist/main.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@emilswork/harness-core": "0.1.0",
    "@emilswork/harness-worker": "0.1.0",
    "better-sqlite3": "^13.0.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/ws": "^8.5.13"
  }
}
